# 文档、目录树、ADR 与规则维护

## 1. 基本原则

1. 长期文档只记录稳定事实、规则、决策和背景。
2. 任务过程和验证结果应进入报告，不写入长期规则、ADR、需求文档或目录树。
3. 不把未验证项写成通过。
4. 不重复维护两套规则。
5. 规则文档保持单一来源和通用表达；语言差异作为通用规则下的分节处理。
6. 文档、需求和报告不得把 mock、fake、stub、health check、脚本存在、局部通过或未执行项写成真实闭环。

## 2. 目录树

1. 项目目录树应保持文件级条目。
2. 新增、删除、移动、重命名文件，或文件职责变化时，必须判断是否更新目录树。
3. 每个 item 职责说明应简短。
4. 不得只写到目录层级。
5. 目录树只用于导航，不能替代读取真实源码、测试、配置和 schema。

## 3. ADR

影响长期架构、接口契约、schema、部署策略、运行时能力声明、验证口径或 rejected option 时，应判断是否需要 ADR。

## 4. 需求与状态跟踪

1. 需求或任务状态变化时，应按项目规则更新跟踪表或等价索引。
2. 不得把 skipped、mock、fake、health check、TCP connect、脚本存在、局部通过或 NOT_RUN 写成真实通过。
3. 发现状态高估时，必须降级或改为明确的未验证说明。
4. 状态必须和实际验证类型一致。

## 5. 报告归档

1. 报告归档是文档维护的一部分，不需要重复设置第二套报告流程。
2. 报告必须遵守 `reporting.md` 的语言、位置、命名、格式和结果规则。
