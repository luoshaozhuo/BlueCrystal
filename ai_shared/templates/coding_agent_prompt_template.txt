# Coding Agent Prompt 模板生成说明

本文件给网页版 LLM 使用，用于生成交给 Claude Code / OpenAI Codex 的执行 prompt。

## 1. 不要重复的内容

生成 prompt 时，不要重复以下固定规则正文：

```text
1. 不要重复 CLAUDE.md 中的固定编码流程。
2. 不要重复 ai_shared/rules/reporting.md 中的最终反馈格式。
3. 不要重复 ai_shared/rules/coding.md / testing.md / validation-routing.md / quality-gate.md 的完整规则。
4. 不要重复 skill 全文。
5. 不要写 report 模板。
```

生成 prompt 只需要写清楚本轮任务目标、事实基线、必须读取的上下文、允许/禁止修改范围、三个 subagent 的 handoff 内容、验收标准和报告目标。

## 2. 固定 agent 流程

按 `task_tier` 分级：

```text
- light: 主会话直接处理；不委派任何 subagent
- standard: @agent-code-implementer → 主会话收口
- full: @agent-code-implementer → @agent-test-validator → @agent-project-steward → 主会话收口
```

`tier_hint` 由 `changed-files-gate` 给出；主会话可在白名单命中时强制覆盖到 `full`。

## 3. 可用 skills

```text
changed-files-gate
code-quality-gate
project-tree-update
project-tree-reset
requirement-trace
rule-update
heavy-regression
commit-message
```

说明：

```text
1. project-tree-read 不再作为 skill；读取 project_tree 是普通导航规则。
2. report-archive 不再作为 skill；报告归档由 project-steward 按 reporting.md 直接写入 ai_shared/reports/。
3. feedback-archive 不再作为 skill；用户反馈归档归入 reporting.md。
4. 只在 handoff 中引用需要的 skill，不复制 skill 细则。
5. `project-tree-update` 和 `project-tree-reset` 仅在用户明确要求时列入，handoff 必须转述该请求，不根据文件变化自动检查或触发。
```

## 4. 可用 memory / 文档入口

按任务需要选择，不要默认全读：

```text
ai_shared/memory/project_tree.md
ai_shared/memory/*REQ*.md
ai_shared/memory/*项目说明*.md
ai_shared/reports/<上一轮报告>.md
```

注意：

```text
1. project_tree 只用于定位，不能替代源码读取。
2. 需求和报告只在本轮需要时列入 required files。
3. 真实事实必须以当前源码、测试、配置、schema 为准。
```

## 5. Prompt 输出格式

---

# 第{ROUND_NO}轮_共{TOTAL_ROUNDS}轮：{TASK_TITLE}

> task_tier: {TASK_TIER}（light / standard / full）

## 0. 本轮唯一目标

```text
{ONE_SENTENCE_GOAL}
```

## 1. 当前事实基线

```text
1. {BASELINE_1}
2. {BASELINE_2}
3. {BASELINE_3}
```

## 2. 必读上下文

只读取以下文件；不要默认读取全部 docs / reports / project_tree。

```text
CLAUDE.md
ai_shared/rules/routing.md
{REQUIRED_CONTEXT_1}
```

说明：

```text
1. 如果使用 project_tree，只能用于导航。
2. 定位后必须二次读取真实源码、测试、配置、schema。
3. 如果文件不存在，必须先说明，不得凭记忆补。
```

## 3. 允许修改

```text
{ALLOWED_PATH_1}
```

## 4. 禁止修改

```text
{FORBIDDEN_PATH_1}
```

## 5. @agent-code-implementer handoff

```text
Agent handoff:
- task_tier: {TASK_TIER}
- task:
  - {IMPLEMENT_TASK}
- allowed paths:
  - {ALLOWED_PATH_1}
- forbidden:
  - {FORBIDDEN_PATH_1}
- required files:
  - ai_shared/rules/routing.md
  - {IMPLEMENT_REQUIRED_FILE_1}
- changed files:
  - 使用 changed-files-gate，编码前后各执行一次
- evidence available:
  - {IMPLEMENT_EVIDENCE_1}
- rules to read:
  - routing 指定的 coding / testing / validation / quality / comment-doc 规则
  - task_tier=standard: 跳过 validation-routing.md 全文
  - task_tier=full: 全读
- skills to use:
  - changed-files-gate
  - code-quality-gate
- commands to run:
  - {IMPLEMENT_COMMAND_1}
- expected output:
  - task_tier=standard: 4 字段 Agent result（reporting.md §2.2）
  - task_tier=full: 12 字段 Agent result（reporting.md §2.1）
```

> 本节仅在 `task_tier` 为 `standard` 或 `full` 时生效；`light` 任务不委派 `code-implementer`。

## 6. @agent-test-validator handoff

```text
Agent handoff:
- task_tier: full
- task:
  - 独立验证 code-implementer 的真实变更
- allowed paths:
  - read-only
- forbidden:
  - 不得修改源码、测试、文档
- required files:
  - ai_shared/rules/testing.md
  - ai_shared/rules/validation-routing.md
  - ai_shared/rules/quality-gate.md
  - ai_shared/rules/python-docstring-cn.md
  - {VALIDATE_REQUIRED_FILE_1}
- changed files:
  - 使用 changed-files-gate 获取真实变更文件
- evidence available:
  - code-implementer Agent result
- rules to read:
  - testing
  - validation-routing
  - quality-gate
  - comment-doc
- skills to use:
  - changed-files-gate
  - code-quality-gate
- commands to run:
  - {TEST_COMMAND_1}
- expected output:
  - 12 字段 Agent result
```

> 本节仅在 `task_tier=full` 时生效；`standard` / `light` 任务不委派 `test-validator`。

## 7. @agent-project-steward handoff

```text
Agent handoff:
- task_tier: full
- task:
  - 根据已验证结果更新文档、需求跟踪和报告；目录树仅按明确指令维护
- allowed paths:
  - ai_shared/
- forbidden:
  - src/
  - tests/
  - tools/
  - 除非本 handoff 明确授权，不得改源码和测试
- required files:
  - ai_shared/rules/documentation.md
  - ai_shared/rules/reporting.md
  - {DOC_REQUIRED_FILE_1}
- changed files:
  - 使用 changed-files-gate 获取真实变更文件
- evidence available:
  - code-implementer Agent result
  - test-validator Agent result
- rules to read:
  - documentation
  - reporting
- skills to use:
  - changed-files-gate
  - requirement-trace if needed
  - rule-update if needed
- commands to run:
  - {DOC_COMMAND_1}
- report target:
  - ai_shared/reports/{REPORT_FILE_NAME}.md
- expected output:
  - 12 字段 Agent result
```

> 本节仅在 `task_tier=full` 时生效；`standard` 仅在需求状态变化或规则变化时启动；`light` 不启动 `project-steward`。如用户明确要求目录树操作，再将该请求、对应 skill 和 `project_tree.md` 加入 handoff。

## 8. 验收标准

```text
1. 本轮引入 failed = 0，或明确 environment-pending。
2. 改动已按语言和影响范围执行最小必要验证：语法/编译、lint、type-check、受影响测试与必要边界验证。
3. 行为变化有测试证据。
4. 未经用户明确要求时，未检查或执行 project-tree-update / project-tree-reset。
5. 需求状态没有高估。
6. 报告和最终反馈能追溯到 Agent result。
7. 未默认扩大到回归、长测、性能、完整部署验证；若执行高成本验证，必须说明触发原因。
```
