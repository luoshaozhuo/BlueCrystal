# 第{ROUND_NO}轮_共{TOTAL_ROUNDS}轮：{TASK_TITLE}

## 任务定位

本轮是“{PROJECT_OR_STAGE_NAME}”的第 {ROUND_NO} 轮。

上一轮 / 当前基线：

```text
1. {BASELINE_1}
2. {BASELINE_2}
3. {BASELINE_3}
4. {BASELINE_4}
```

本轮目标：{ONE_SENTENCE_GOAL}

---

## 总目标

本轮结束时，至少达到：

```text
1. {GOAL_1}
2. {GOAL_2}
3. {GOAL_3}
4. {GOAL_4}
5. 已完成能力不回退。
6. 不支持能力必须明确 NOT_IMPLEMENTED / limitation / skip reason。
7. failed = 0。
8. ADR / project_tree / report 更新。
```

如存在环境依赖或不可规避限制，必须满足：

```text
1. 明确缺失依赖或阻塞点。
2. 给出可复现构建 / 运行 / 测试命令。
3. 不把 skip / NOT_IMPLEMENTED / 环境缺失写成 PASS。
4. 不影响本轮已可验证能力的通过。
```

---

## 必须遵守的边界

1. 不修改 {BOUNDARY_1}。
2. 不修改 {BOUNDARY_2}。
3. 不新增 {FORBIDDEN_NEW_CAPABILITY}。
4. 不恢复已删除目录 / 旧路径 / shim。
5. 不允许 fake OK。
6. 不允许用 health check / TCP connect 替代真实业务验证。
7. 不允许主流程堆协议 / 类型 / 场景 if/else。
8. 不允许带 failed 收口。
9. 不重构非本轮目标范围内的架构。
10. 允许为本轮目标做必要的实现、fixture、路径、stdout parser、测试、文档小修。
11. 必须保持已完成能力不回退。

---

## 必须读取

先读取以下文件，不要凭记忆判断：

```text
CLAUDE.md
AGENTS.md
ai_shared/memory/project_tree.md
ai_shared/adr/ADR索引.md

{ADR_OR_RULE_FILE_1}
{ADR_OR_RULE_FILE_2}
{ADR_OR_RULE_FILE_3}

{PREVIOUS_REPORT_1}
{PREVIOUS_REPORT_2}
{PREVIOUS_REPORT_3}
```

如果文件名不同，以实际目录为准搜索：

```text
{SEARCH_KEYWORD_1}
{SEARCH_KEYWORD_2}
{SEARCH_KEYWORD_3}
```

---

## 必须读取源码

```text
{SOURCE_FILE_OR_DIR_1}
{SOURCE_FILE_OR_DIR_2}
{SOURCE_FILE_OR_DIR_3}
{SOURCE_FILE_OR_DIR_4}
{SOURCE_FILE_OR_DIR_5}
```

如路径变化，先搜索：

```text
{CODE_SEARCH_KEYWORD_1}
{CODE_SEARCH_KEYWORD_2}
{CODE_SEARCH_KEYWORD_3}
{CODE_SEARCH_KEYWORD_4}
```

---

## 工作项 A：{WORK_ITEM_A_TITLE}

当前问题：

```text
{WORK_ITEM_A_PROBLEM}
```

本轮要求：

1. {A_REQUIREMENT_1}
2. {A_REQUIREMENT_2}
3. {A_REQUIREMENT_3}
4. 不允许 {A_FORBIDDEN_1}。
5. 必须通过真实链路 / 真实数据 / 真实协议 / 真实接口验证。
6. 如做不到，必须降级为 NOT_IMPLEMENTED / limitation，并说明具体技术原因。

新增或补充测试：

```text
{A_TEST_FILE_1}
{A_TEST_FILE_2}
```

至少覆盖：

```text
test_{A_TEST_CASE_1}
test_{A_TEST_CASE_2}
test_{A_TEST_CASE_3}
```

验收：

```text
1. {A_ACCEPTANCE_1}
2. {A_ACCEPTANCE_2}
3. {A_ACCEPTANCE_3}
```

---

## 工作项 B：{WORK_ITEM_B_TITLE}

当前问题：

```text
{WORK_ITEM_B_PROBLEM}
```

本轮要求：

1. {B_REQUIREMENT_1}
2. {B_REQUIREMENT_2}
3. {B_REQUIREMENT_3}
4. 不允许 {B_FORBIDDEN_1}。
5. 输出必须可测试、可复现、可报告。

新增或补充测试：

```text
{B_TEST_FILE_1}
{B_TEST_FILE_2}
```

至少覆盖：

```text
test_{B_TEST_CASE_1}
test_{B_TEST_CASE_2}
test_{B_TEST_CASE_3}
```

---

## 工作项 C：矩阵 / 门禁 / 回归扩展

将当前验证矩阵扩展为：

```text
{MATRIX_ITEM_1}
{MATRIX_ITEM_2}
{MATRIX_ITEM_3}
{MATRIX_ITEM_4}
```

要求：

1. 已通过项必须继续 PASS。
2. 新增项必须进入自动化测试。
3. 每个项必须包含：
   - status；
   - runner / adapter / facade / provider 类型；
   - 关键指标；
   - skip reason，如有。
4. 不允许因为某个可选项缺环境导致全套失败；但真实代码错误必须 failed。
5. 不支持能力必须明确 NOT_IMPLEMENTED，不得隐式成功。

---

## 工作项 D：架构边界回归

继续保证：

```text
{ARCH_FILE_1}
{ARCH_FILE_2}
{ARCH_FILE_3}
```

要求：

1. 主流程不得出现协议 / 类型 / 场景 if/else 堆。
2. 差异只能集中在：
   - factory；
   - registry；
   - mapping；
   - protocol-specific / adapter-specific / provider-specific 层；
   - fixture 层。
3. 保持或新增静态 / AST / import boundary 检查。
4. 不允许将测试工具层泄漏到生产 use case。
5. 不允许将生产 client 逻辑搬进 simulator / task facade 层。

---

## 工作项 E：NOT_IMPLEMENTED / limitation / capability 矩阵

要求：

1. 本轮新增能力必须写入 capability 矩阵。
2. 未实现能力继续明确 NOT_IMPLEMENTED。
3. 环境依赖必须写清 skip reason 和 CI 条件。
4. capability 不得声明超前。
5. 新增测试防止：
   - supports_xxx=True 但无真实 smoke；
   - E2E PASS 但关键指标为空；
   - NOT_IMPLEMENTED 被吞掉为 OK；
   - skip 被写成 PASS。

---

## 工作项 F：构建 / 运行 / CI 命令

本轮必须写清构建与 CI 条件。

要求：

1. 列出构建命令：

```bash
{BUILD_COMMAND_1}
{BUILD_COMMAND_2}
```

2. 列出测试命令：

```bash
{TEST_COMMAND_1}
{TEST_COMMAND_2}
```

3. 如果 target / 命令名称不同，以实际为准。
4. 报告当前环境可用 / 不可用原因。
5. 不把环境缺失伪装为功能通过。

---

## 工作项 G：回归已有能力

必须保证以下不回退：

```text
1. {REGRESSION_ITEM_1}
2. {REGRESSION_ITEM_2}
3. {REGRESSION_ITEM_3}
4. {REGRESSION_ITEM_4}
5. NOT_IMPLEMENTED 验收。
6. 目录结构 / 旧路径不回归。
7. failed = 0。
```

---

## 工作项 H：ADR / project_tree / report

更新：

```text
ai_shared/adr/{ADR_FILE_TO_UPDATE}
ai_shared/memory/project_tree.md
```

ADR 增加 Implementation Status：

```text
Round {ROUND_NO}: {ADR_STATUS_TITLE}
```

必须记录：

1. {ADR_RECORD_1}
2. {ADR_RECORD_2}
3. {ADR_RECORD_3}
4. 未完成项与原因。
5. 对后续轮次的影响。

新增报告：

```text
ai_shared/reports/{REPORT_FILE_NAME}.md
```

如新增 / 删除 / 移动文件，必须同步 `project_tree.md`。

---

## 允许修改

```text
{ALLOWED_PATH_1}
{ALLOWED_PATH_2}
{ALLOWED_PATH_3}
{ALLOWED_PATH_4}
ai_shared/adr/
ai_shared/memory/project_tree.md
ai_shared/reports/
```

必要时可小范围修复：

```text
{OPTIONAL_ALLOWED_PATH_1}
{OPTIONAL_ALLOWED_PATH_2}
```

但不得改变本轮禁止修改的核心边界。

---

## 禁止事项

```text
1. 禁止修改 {FORBIDDEN_PATH_OR_LAYER_1}。
2. 禁止修改 {FORBIDDEN_PATH_OR_LAYER_2}。
3. 禁止新增 {FORBIDDEN_CAPABILITY_1}。
4. 禁止恢复旧路径 / shim / deprecated fallback。
5. 禁止 fake OK。
6. 禁止用 health check 替代真实业务验证。
7. 禁止主流程堆 if/else。
8. 禁止带 failed 收口。
9. 禁止把环境缺失写成 PASS。
10. 禁止把模拟环境验收写成真实现场硬件验收。
```

---

## 必须执行测试

至少执行：

```bash
{MUST_TEST_COMMAND_1}
{MUST_TEST_COMMAND_2}
{MUST_TEST_COMMAND_3}
{MUST_TEST_COMMAND_4}
```

如果有新增专项测试：

```bash
{OPTIONAL_TEST_COMMAND_1}
{OPTIONAL_TEST_COMMAND_2}
```

如果完整环境可用，额外执行：

```bash
{HEAVY_OR_LOAD_TEST_COMMAND}
```

如重型测试不适合当前环境，报告说明原因并给出 CI 命令。

---

## 验收标准

本轮通过必须满足：

```text
1. {ACCEPTANCE_1}
2. {ACCEPTANCE_2}
3. {ACCEPTANCE_3}
4. 已有能力不回退。
5. 关键指标非空 / 可计算 / 可追踪。
6. 主流程无不合理 if/else 堆。
7. NOT_IMPLEMENTED / limitation / skip reason 准确。
8. failed = 0。
9. 单元 / 集成 / 专项 / 回归测试通过。
10. ADR / project_tree / report 更新。
```

---

## 输出报告

新增：

```text
ai_shared/reports/{REPORT_FILE_NAME}.md
```

报告必须包含：

```text
A. 修改文件清单
B. 本轮目标完成情况
C. 核心能力实现说明
D. E2E / smoke / gate 矩阵
E. 关键指标验证
F. NOT_IMPLEMENTED / limitation / capability 更新
G. 架构边界检查
H. 构建与 CI 命令
I. 回归测试结果
J. ADR / project_tree 更新
K. 剩余风险
L. 下一轮建议
```

---

## 最终反馈格式

```text
修改文件：
- ...

新增文件：
- ...

行为变化：
- ...

{DOMAIN_OBJECT_1}：
- status:
- implementation:
- tests:
- limitation:

{DOMAIN_OBJECT_2}：
- status:
- implementation:
- tests:
- limitation:

矩阵：
- {MATRIX_ITEM_1}:
- {MATRIX_ITEM_2}:
- {MATRIX_ITEM_3}:

架构检查：
- provider/factory/registry:
- main flow:
- NOT_IMPLEMENTED:
- protocol switching / type switching:

检查与测试：
-专项:
- smoke:
- e2e:
- access / source_lab:
- unit:
- integration:
- load / heavy:

ADR 状态：
- ...

project_tree 状态：
- ...

是否本轮收口：
- 是/否
- 理由：

failed/skipped：
- failed:
- 本轮新增 skipped:
- pre-existing skipped:

剩余风险：
- ...

下一步建议：
- ...
```
