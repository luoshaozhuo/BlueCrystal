# Leave empty to use local mock APIs in development.
# Set this to your backend endpoint when needed, for example:
# VITE_API_BASE_URL=http://localhost:8080
VITE_API_BASE_URL=
# Enable only when you want lint checks during dev startup/HMR.
# VITE_ENABLE_ESLINT=true
