# IEC 60870-5-104 工程说明文档（iec104-python 适配版 v1.5.13）

> 本文以原 `IEC_60870-5-104_v1_5_7` 为基线增量修订；数据库模型总览见 `05_bluecrystal_model_v1_5_13.md`。

## 1. 标准概述

IEC 60870-5-104，工程上通常简称 **IEC104**，是电力自动化、调度通信、场站网关和 SCADA 系统中常见的远动通信协议。它在应用层沿用 IEC 60870-5-101 的 ASDU 语义，并将链路承载方式改为 TCP/IP 网络通信。

IEC104 主要用于以下场景：

- 调度主站与变电站、风电场、光伏场站、储能站之间的数据通信；
- 场站网关向调度或集控系统上送遥信、遥测、事件和带时标数据；
- 主站向子站下发遥控、遥调、总召、时钟同步等命令；
- 通过 IP 网络替代传统串口远动通道。

IEC104 的核心不是“按地址读取寄存器”，而是**主站与子站之间交换 ASDU 事件和命令**。主站通常主动建立 TCP 连接，子站通常监听 TCP 端口 `2404`，连接建立后通过 STARTDT 启动数据传输，再进行总召、事件上送、周期上送、遥控遥调等业务流程。

## 2. 角色与工程边界

### 2.1 控制站与被控站

IEC104 中应优先使用以下角色描述：

| 角色 | 常用中文 | 典型工程对象 | 常见 TCP 行为 |
|------|----------|--------------|---------------|
| **Controlling Station** | 主站 / 控制站 | 调度主站、集控系统、数据采集平台 | TCP Client，主动连接 |
| **Controlled Station** | 子站 / 被控站 | RTU、场站网关、升压站监控系统 | TCP Server，监听 2404 |

在多数风电场、光伏场站、储能场站接入场景中，场站侧程序属于：

```text
IEC104 Controlled Station / Server
```

也就是：

```text
主站主动连接场站网关；
场站网关监听 TCP 2404；
场站网关响应 STARTDT、总召、时钟同步、遥控遥调；
场站网关主动上送遥信、遥测、事件和周期数据。
```

因此，面向场站侧开发时，示例代码应重点放在 **子站端 / controlled station / server**，而不是主站端 client。

### 2.2 “Client” 一词的歧义

部分软件库会把“主动建立 TCP 连接的一方”称为 client，把“监听端口的一方”称为 server。这种说法来自 TCP 编程模型。

IEC104 工程文档中更建议按业务角色区分：

| 语境 | Client 可能含义 | 推荐写法 |
|------|----------------|----------|
| TCP 编程 | 主动连接方 | TCP client |
| IEC104 业务 | 主站 / 控制站 | controlling station |
| 场站侧开发 | 子站 / 被控站 | controlled station / server |

如果场站侧程序因网络架构原因主动连接中心服务，也不能简单称为“IEC104 主站”。它可能只是 TCP 连接发起方，但在 IEC104 应用语义上仍承担 controlled station 的业务角色。

## 3. 核心概念体系

### 3.1 APDU、APCI、ASDU

IEC104 报文基本单元是 **APDU**：

```text
APDU = APCI + ASDU
```

更完整地看：

```text
APDU
├── Start Character       1 byte   固定 0x68
├── APDU Length           1 byte   后续长度，不含 Start 和 Length
├── APCI / Control Field  4 bytes  控制域，I/S/U 格式
└── ASDU                  n bytes  应用服务数据单元
    ├── Type Identification       1 byte
    ├── VSQ                       1 byte
    ├── Cause of Transmission     1 or 2 bytes，IEC104 常用 2 bytes
    ├── Common Address of ASDU    1 or 2 bytes，IEC104 常用 2 bytes
    └── Information Object(s)
        ├── Information Object Address  1/2/3 bytes，IEC104 常用 3 bytes
        └── Information Elements        n bytes，由 Type ID 决定
```

真正的数据值不在一个单独叫 `data` 的字段里，而是在：

```text
ASDU -> Information Object -> Information Elements
```

IEC104 常用 APDU 最大长度为 253 字节。该长度包含 4 字节 APCI 和后续 ASDU，不包含最前面的 `0x68` 与 Length 字节。

### 3.2 ASDU 公共地址与 IOA

IEC104 点的定位通常依赖：

```text
Common Address of ASDU + Type ID + IOA
```

| 字段 | 含义 |
|------|------|
| `common_address` / `station` / `ca` | ASDU 公共地址，表示逻辑站、逻辑单元或被控系统分区 |
| `type_id` | ASDU Type Identification，表示数据结构和编码方式 |
| `ioa` | Information Object Address，信息对象地址 |

`common_address` 是协议逻辑地址，不等同于 IP 地址，也不必等同于物理设备。一条 TCP 连接下可以承载多个 Common Address。

例如：

```text
192.168.1.10:2404
  ├── CA=1 风电场总控
  ├── CA=2 升压站
  ├── CA=3 SVG/无功补偿
  └── CA=4 储能系统
```

这表示一个 IEC104 子站服务端代理多个逻辑站或逻辑系统。

IEC104 普通数据 ASDU 不携带字段名、中文名、单位或业务语义。IOA 相当于协议点号，但完整解释依赖 `Common Address + Type ID + IOA`，通信双方必须通过点号表或工程联调文件约定其含义。

### 3.2.1 一个 ASDU 携带多个信息对象

同一个 ASDU 可以携带多个信号，但这些信息对象必须共享同一个：

```text
Type ID + COT + Common Address
```

因此不能在同一个 ASDU 中混合不同 Type ID、不同 COT 或不同 Common Address。

`SQ=0` 时，每个信息对象都携带自己的 IOA，地址可以不连续：

```text
ASDU
├── Type ID
├── VSQ: SQ=0, count=3
├── COT
├── Common Address
├── IOA=1001 + Information Element
├── IOA=1005 + Information Element
└── IOA=1020 + Information Element
```

`SQ=1` 时，仅第一个信息对象携带 IOA，后续地址顺序递增：

```text
ASDU
├── Type ID
├── VSQ: SQ=1, count=3
├── COT
├── Common Address
├── First IOA=1001
├── Information Element for IOA=1001
├── Information Element for IOA=1002
└── Information Element for IOA=1003
```

子站运行时应先按 `(session, Common Address, Type ID, COT)` 分组，再依据 APDU 长度限制分帧。

### 3.3 Type ID

`type_id` 是 IEC104 ASDU 的 **Type Identification**，决定 ASDU 中 Information Element 的编码格式。

常用 Type ID：

| Type ID | 数值 | 含义 | 方向 |
|---------|------|------|------|
| `M_SP_NA_1` | 1 | 单点遥信，不带时间 | monitor |
| `M_SP_TB_1` | 30 | 单点遥信，带 CP56Time2a 时间 | monitor |
| `M_DP_NA_1` | 3 | 双点遥信，不带时间 | monitor |
| `M_DP_TB_1` | 31 | 双点遥信，带 CP56Time2a 时间 | monitor |
| `M_ME_NA_1` | 9 | 归一化遥测，不带时间 | monitor |
| `M_ME_NB_1` | 11 | 标度化遥测，不带时间 | monitor |
| `M_ME_NC_1` | 13 | 短浮点遥测，不带时间 | monitor |
| `M_ME_TF_1` | 36 | 短浮点遥测，带 CP56Time2a 时间 | monitor |
| `M_IT_NA_1` | 15 | 累计量，不带时间 | monitor |
| `M_IT_TB_1` | 37 | 累计量，带 CP56Time2a 时间 | monitor |
| `M_EI_NA_1` | 70 | 初始化结束 | monitor |
| `C_SC_NA_1` | 45 | 单点遥控命令 | control |
| `C_DC_NA_1` | 46 | 双点遥控命令 | control |
| `C_SE_NA_1` | 48 | 归一化设点命令 | control |
| `C_SE_NB_1` | 49 | 标度化设点命令 | control |
| `C_SE_NC_1` | 50 | 短浮点设点命令 | control |
| `C_IC_NA_1` | 100 | 总召唤命令 | control |
| `C_CI_NA_1` | 101 | 累计量召唤命令 | control |
| `C_RD_NA_1` | 102 | 读命令 | control |
| `C_CS_NA_1` | 103 | 时钟同步命令 | control |

`type_id` 不是 `spontaneous`、`cyclic` 或 `interrogation`。这些属于传送原因或触发策略，最终体现在 COT 中。

### 3.4 COT：Cause of Transmission

COT 表示本次 ASDU 发送的原因。

常见 COT：

| COT | 含义 |
|-----|------|
| `cyclic` | 周期上送 |
| `background` | 背景扫描 |
| `spontaneous` | 自发上送，通常由值变化触发 |
| `activation` | 激活命令 |
| `activation_confirmation` | 激活确认 |
| `activation_termination` | 激活终止 |
| `interrogated_by_station` | 响应站总召 |
| `interrogated_by_group_1` ~ `interrogated_by_group_16` | 响应第 1~16 组召唤 |

同一个监视点可以使用同一个 Type ID，在不同场景下带不同 COT 发出。

例如 `wind_speed`：

```text
总召响应：Type ID = M_ME_TF_1，COT = interrogated_by_station
周期上送：Type ID = M_ME_TF_1，COT = cyclic
变化上送：Type ID = M_ME_TF_1，COT = spontaneous
```

### 3.5 QOI：仅存在于召唤命令信息体

QOI（Qualifier of Interrogation）**携带在 ASDU 中**，但它不是所有 ASDU 都有的公共头字段，而是 `C_IC_NA_1` 召唤命令 Information Object 的 Information Element。

```text
C_IC_NA_1 ASDU
├── Type ID = C_IC_NA_1
├── VSQ
├── COT = activation / activation_confirmation / activation_termination
├── Common Address
└── Information Object
    ├── IOA = 0
    └── QOI
        ├── 20：站总召
        └── 21～36：第 1～16 组召唤
```

总召或组召返回的遥信、遥测数据 ASDU **不再携带 QOI**。它们通过 COT 表明来源：

```text
QOI=20 触发的数据：COT = interrogated_by_station
QOI=21～36 触发的数据：COT = interrogated_by_group_1～16
```

标准允许组召，但当前未修改的 iec104-python Server 不实现 QOI=21～36；本项目以多个 Common Address / Station 替代多个总召点集。

## 4. 协议栈与帧格式

### 4.1 协议栈

| 层级 | 内容 |
|------|------|
| 应用层 | IEC 60870-5-104 ASDU，遥信、遥测、遥控、总召、时钟同步等 |
| 控制层 | APCI，I/S/U 帧、发送序号、接收序号、STARTDT、STOPDT、TESTFR |
| 传输层 | TCP，常用端口 `2404` |
| 网络层 | IP |
| 数据链路/物理层 | Ethernet、光纤、专线、VPN、4G/5G 专网等 |

IEC104 不再使用 IEC101 的串口链路层帧格式，而是通过 TCP 承载 APDU。

### 4.2 I-format 信息帧

I-format 用于承载业务 ASDU，是遥信、遥测、命令响应等数据的主要帧格式。

I-format APCI 控制域共 4 字节：

```text
Byte0-1: 发送序号 N(S)，左移 1 位后小端编码
Byte2-3: 接收序号 N(R)，左移 1 位后小端编码
```

示例：

```text
N(S) = 1
N(R) = 0
N(S) << 1 = 0x0002
N(R) << 1 = 0x0000
```

控制域为：

```text
02 00 00 00
```

一个上送单点遥信的完整信息帧示例：

```text
68 0E 02 00 00 00 01 01 03 00 01 00 E9 03 00 01
│  │  └───────────┘ └─────────────────────────────┘
│  │       APCI                   ASDU
│  │
│  └─ Length = 0x0E = 14 bytes = APCI 4 + ASDU 10
│
└─ Start = 0x68
```

拆解：

```text
68          Start
0E          Length = 14
02 00       Send sequence N(S)=1
00 00       Receive sequence N(R)=0

01          Type ID = 1 = M_SP_NA_1
01          VSQ = 1 个信息对象
03 00       COT = 3 = spontaneous
01 00       Common Address = 1
E9 03 00    IOA = 1001，小端 3 字节
01          SIQ，单点状态 = ON
```

该报文表示：

```text
子站 CA=1 自发上送一个单点遥信：IOA=1001，值为 ON。
```

### 4.3 S-format 与 U-format

| 帧类型 | 用途 |
|--------|------|
| I-format | 承载 ASDU 业务数据，带 N(S)、N(R) |
| S-format | 仅确认接收序号 N(R)，不承载 ASDU |
| U-format | 控制链路状态，如 STARTDT、STOPDT、TESTFR |

常见 U-format：

| 命令 | 含义 |
|------|------|
| `STARTDT_ACT` | 主站请求启动数据传输 |
| `STARTDT_CON` | 子站确认启动数据传输 |
| `STOPDT_ACT` | 请求停止数据传输 |
| `STOPDT_CON` | 确认停止数据传输 |
| `TESTFR_ACT` | 测试帧请求 |
| `TESTFR_CON` | 测试帧确认 |

### 4.3 STARTDT_ACT 与 COT=activation

二者不是同一层级：

- `STARTDT_ACT` 是 U-format 链路控制，用于启动当前 TCP 会话的数据传输。
- `COT=activation` 是应用层命令语义，表示请求对端执行总召、遥控、遥调、时钟同步等 ASDU 所表达的操作。

典型应用层流程为：

```text
activation
→ activation_confirmation
→ 执行命令或返回数据
→ activation_termination
```

## 5. 总召与数据上送机制

### 5.1 总召的含义

总召是主站向子站发送 `C_IC_NA_1`，请求子站上送当前参与总召的信息对象快照。

总召不是广播，也不是订阅。它是一次性全量快照请求。

典型流程：

```text
主站 -> 子站：C_IC_NA_1，COT=activation，QOI=20
子站 -> 主站：C_IC_NA_1，COT=activation_confirmation
子站 -> 主站：参与总召的遥信/遥测/累计量，COT=interrogated_by_station
子站 -> 主站：C_IC_NA_1，COT=activation_termination
```

### 5.2 总召一次后的发送行为

总召一次，子站只完成一次总召响应流程。

如果点很多，子站可能连续发送多个 I-format 帧；如果点少，可能一个 ASDU 就发完。无论是一帧还是多帧，都属于本次总召响应。

总召完成后，子站不会因为这次总召而持续不断发送同一批数据。后续持续更新依赖以下机制：

| 机制 | 是否由总召建立 | 含义 |
|------|----------------|------|
| 总召响应 | 是 | 本次总召触发的一轮当前值上送 |
| 自发上送 | 否 | 值变化或状态变化时主动上送 |
| 周期上送 | 否 | 按子站配置的周期主动上送 |
| 背景扫描 | 否 | 按后台扫描策略上送 |
| 周期性总召 | 否 | 主站定时再次发送总召命令 |

### 5.2.1 背景扫描

背景扫描是子站按内部低优先级、低频刷新策略上送当前值，COT=`background`。它不是值变化触发、不是总召响应，也不是主站建立的订阅。

### 5.3 `iec104-python` 的实际召唤能力

IEC 60870-5-104 标准定义：

| QOI | 标准含义 |
|---|---|
| 20 | 站总召 |
| 21～36 | 第 1～16 组召 |

但本项目直接使用未修改的 `iec104-python`。当前服务端实现只处理 `QOI=20`：

```text
收到 C_IC_NA_1
→ 校验 Common Address
→ 仅接受 QOI=20
→ 发送 positive activation confirmation
→ 遍历目标 Station 已注册的 Point
→ 仅发送当前实现支持参与站总召的监视 Type ID
→ COT 固定为 INTERROGATED_BY_STATION
→ 发送 activation termination
```

`QOI=21～36` 在当前服务端源码中标记为未实现，收到后返回否定激活确认，不返回组召点集。因此数据库不得建立可执行的组召响应任务。

### 5.4 多 Common Address 替代多组总召

本项目不修改 `iec104-python`，使用不同 Common Address 建立多个逻辑 Station：

```text
同一 TCP Server
├── CA=1 / Station 1：总召点集 A
└── CA=2 / Station 2：总召点集 B
```

主站分别向 CA=1、CA=2 发送 `C_IC_NA_1 + QOI=20`，即可获得不同点集。该方案的工程含义是两个 IEC104 逻辑 Station，而不是同一 Station 的两个组召组。

同一业务信号若同时出现在两个总召点集中，需要建立两条 Point：

```text
CA=1, IOA=1001
CA=2, IOA=1001
```

两条 Point 可以引用同一个业务语义和同一个本地数据源，但在 IEC104 地址空间中是两个不同信息对象。

### 5.5 站总召实际选点规则

`iec104-python` 的 Point 没有以下属性：

```text
interrogation_enabled
include_in_general_interrogation
qoi
group
interrogation_groups
```

当前服务端站总召的选点条件是：

```text
请求 CA 匹配 Station
AND Point 已注册到该 Station
AND Point Type ID 属于服务端站总召实现支持的监视类型
```

当前实现参与普通站总召的 Type ID 数值范围为：

```text
1～13
30～36
```

即单点、双点、步位置、比特串和测量值相关类型。以下类型不会进入当前普通站总召路径：

- `M_IT_*`：累计量，应使用累计量召唤路径；
- `M_EP_*`：保护事件；
- `M_PS_NA_1`：成组状态；
- `M_ME_ND_1`；
- `M_EI_NA_1`：初始化结束。

因此“监视方向”不等同于“当前包支持参与站总召”。数据库以 `cfg_iec104_type_def.general_interrogation_supported` 为权威依据。

## 6. 数据上送机制与数据库边界

### 6.1 CYCLIC

`cfg_iec104_point_item.report_ms` 直接对应 `iec104-python` Point 的周期报告参数：

```text
report_ms = 0     不启用包内置周期发送
report_ms > 0     由 Server 按该 Point 周期自动发送，COT=CYCLIC
```

周期是 Point 级配置，不在任务参数中重复维护。`IEC104_SEND_CYCLIC_DATA` 任务用于组织需要初始化为周期报告的 Point 集合，其触发方式是服务启动事件，而不是应用层定时轮询。

### 6.2 SPONTANEOUS

当前包没有内置以下能力：

```text
值变化检测
死区判断
最小发送间隔
last_send_at
last_update_at
```

Point 也不支持任意动态增加属性。因此 Adapter 必须在独立运行时状态中维护：

```text
last_observed_value
last_spontaneous_value
last_spontaneous_sent_at
```

数据库点级配置：

```text
spontaneous_deadband
spontaneous_min_interval_ms
```

其中：

```text
spontaneous_min_interval_ms = 0  不启用自发上送
spontaneous_min_interval_ms > 0  值变化超过 deadband 且满足最小间隔时调用 transmit
```

### 6.3 BACKGROUND_SCAN

背景扫描不是值变化触发，也不是包内置 `report_ms` 周期。它表示一个任务点集按固定时间间隔进行低优先级全量刷新，并以 `COT=BACKGROUND` 发送。

背景周期属于任务级参数，Point 表不重复保存。Adapter 定时遍历 `task_point_table_id` 对应的全部 Point，并调用单点或批量发送 API。

### 6.4 三种方式对比

| 机制 | 触发条件 | 配置位置 | 发送范围 | 运行责任 |
|---|---|---|---|---|
| CYCLIC | 每个 Point 的固定周期 | `cfg_iec104_point_item.report_ms` | 参与周期报告的单个 Point | `iec104-python` Server |
| SPONTANEOUS | 变化超过死区且满足最小间隔 | Point 的 deadband 和最小间隔 | 发生有效变化的 Point | Whale Adapter |
| BACKGROUND_SCAN | 任务固定周期 | `task_param_value` | 任务点表全量 Point | Whale Adapter |

### 6.5 Point 控制参数

`cfg_iec104_point_item` 保存与公开 Point API 对应或直接服务 Adapter 的字段：

| 字段 | 含义 |
|---|---|
| `common_address` | Point 所属 Station 的 CA。 |
| `information_object_address` | IOA，范围 `0..16777215`。 |
| `command_mode_ref_id` | 控制点命令模式：`DIRECT` 或 `SELECT_AND_EXECUTE`。 |
| `report_ms` | 包内置 CYCLIC 周期。 |
| `spontaneous_deadband` | Adapter 自发上送死区。 |
| `spontaneous_min_interval_ms` | Adapter 自发上送最小间隔，0 表示关闭。 |
| `related_io_address` | 控制点关联的监视点 IOA。 |
| `related_io_autoreturn` | 命令成功后是否自动发送关联监视点。 |

`related_io_address` 必须引用同一个 Point Table、同一个 Common Address 下的监视 Point。

### 6.6 Type ID 元数据

`cfg_iec104_type_def` 覆盖 `iec104-python` 当前公开的全部 55 个 Type ID：

```text
监视：1～21、30～40
控制：45～51、58～64
初始化：70
系统与召唤：100～107
```

元数据表保存：

- 标准数值；
- 类型类别；
- 信息值结构；
- 时间标签类型；
- 是否可注册为普通 Point；
- 是否参与当前包站总召；
- 是否支持 command mode；
- 是否支持 related IO。

不允许根据 `M_`、`C_` 字符串前缀推断所有能力。

#### 6.6.1 `iec104-python` 全部 Type ID 能力清单

| 数值 | Type ID | 类别 | 时标 | 普通 Point | QOI=20 | 计数召唤 | `report_ms` | 自发适配 | CommandMode | related IO |
|---:|---|---|---|:---:|:---:|:---:|:---:|:---:|:---:|:---:|
| 1 | `M_SP_NA_1` | `PROCESS_MONITOR` | `NONE` | 是 | 是 | — | 是 | 是 | — | — |
| 2 | `M_SP_TA_1` | `PROCESS_MONITOR` | `CP24TIME2A` | 是 | 是 | — | 是 | 是 | — | — |
| 3 | `M_DP_NA_1` | `PROCESS_MONITOR` | `NONE` | 是 | 是 | — | 是 | 是 | — | — |
| 4 | `M_DP_TA_1` | `PROCESS_MONITOR` | `CP24TIME2A` | 是 | 是 | — | 是 | 是 | — | — |
| 5 | `M_ST_NA_1` | `PROCESS_MONITOR` | `NONE` | 是 | 是 | — | 是 | 是 | — | — |
| 6 | `M_ST_TA_1` | `PROCESS_MONITOR` | `CP24TIME2A` | 是 | 是 | — | 是 | 是 | — | — |
| 7 | `M_BO_NA_1` | `PROCESS_MONITOR` | `NONE` | 是 | 是 | — | 是 | 是 | — | — |
| 8 | `M_BO_TA_1` | `PROCESS_MONITOR` | `CP24TIME2A` | 是 | 是 | — | 是 | 是 | — | — |
| 9 | `M_ME_NA_1` | `PROCESS_MONITOR` | `NONE` | 是 | 是 | — | 是 | 是 | — | — |
| 10 | `M_ME_TA_1` | `PROCESS_MONITOR` | `CP24TIME2A` | 是 | 是 | — | 是 | 是 | — | — |
| 11 | `M_ME_NB_1` | `PROCESS_MONITOR` | `NONE` | 是 | 是 | — | 是 | 是 | — | — |
| 12 | `M_ME_TB_1` | `PROCESS_MONITOR` | `CP24TIME2A` | 是 | 是 | — | 是 | 是 | — | — |
| 13 | `M_ME_NC_1` | `PROCESS_MONITOR` | `NONE` | 是 | 是 | — | 是 | 是 | — | — |
| 14 | `M_ME_TC_1` | `PROCESS_MONITOR` | `CP24TIME2A` | 是 | — | — | 是 | 是 | — | — |
| 15 | `M_IT_NA_1` | `COUNTER_MONITOR` | `NONE` | 是 | — | 是 | 是 | 是 | — | — |
| 16 | `M_IT_TA_1` | `COUNTER_MONITOR` | `CP24TIME2A` | 是 | — | 是 | 是 | 是 | — | — |
| 17 | `M_EP_TA_1` | `PROTECTION_MONITOR` | `CP24TIME2A` | 是 | — | — | 是 | 是 | — | — |
| 18 | `M_EP_TB_1` | `PROTECTION_MONITOR` | `CP24TIME2A` | 是 | — | — | 是 | 是 | — | — |
| 19 | `M_EP_TC_1` | `PROTECTION_MONITOR` | `CP24TIME2A` | 是 | — | — | 是 | 是 | — | — |
| 20 | `M_PS_NA_1` | `PROCESS_MONITOR` | `NONE` | 是 | — | — | 是 | 是 | — | — |
| 21 | `M_ME_ND_1` | `PROCESS_MONITOR` | `NONE` | 是 | — | — | 是 | 是 | — | — |
| 30 | `M_SP_TB_1` | `PROCESS_MONITOR` | `CP56TIME2A` | 是 | 是 | — | 是 | 是 | — | — |
| 31 | `M_DP_TB_1` | `PROCESS_MONITOR` | `CP56TIME2A` | 是 | 是 | — | 是 | 是 | — | — |
| 32 | `M_ST_TB_1` | `PROCESS_MONITOR` | `CP56TIME2A` | 是 | 是 | — | 是 | 是 | — | — |
| 33 | `M_BO_TB_1` | `PROCESS_MONITOR` | `CP56TIME2A` | 是 | 是 | — | 是 | 是 | — | — |
| 34 | `M_ME_TD_1` | `PROCESS_MONITOR` | `CP56TIME2A` | 是 | 是 | — | 是 | 是 | — | — |
| 35 | `M_ME_TE_1` | `PROCESS_MONITOR` | `CP56TIME2A` | 是 | 是 | — | 是 | 是 | — | — |
| 36 | `M_ME_TF_1` | `PROCESS_MONITOR` | `CP56TIME2A` | 是 | 是 | — | 是 | 是 | — | — |
| 37 | `M_IT_TB_1` | `COUNTER_MONITOR` | `CP56TIME2A` | 是 | — | 是 | 是 | 是 | — | — |
| 38 | `M_EP_TD_1` | `PROTECTION_MONITOR` | `CP56TIME2A` | 是 | — | — | 是 | 是 | — | — |
| 39 | `M_EP_TE_1` | `PROTECTION_MONITOR` | `CP56TIME2A` | 是 | — | — | 是 | 是 | — | — |
| 40 | `M_EP_TF_1` | `PROTECTION_MONITOR` | `CP56TIME2A` | 是 | — | — | 是 | 是 | — | — |
| 45 | `C_SC_NA_1` | `CONTROL_COMMAND` | `NONE` | 是 | — | — | — | — | 是 | 是 |
| 46 | `C_DC_NA_1` | `CONTROL_COMMAND` | `NONE` | 是 | — | — | — | — | 是 | 是 |
| 47 | `C_RC_NA_1` | `CONTROL_COMMAND` | `NONE` | 是 | — | — | — | — | 是 | 是 |
| 48 | `C_SE_NA_1` | `CONTROL_COMMAND` | `NONE` | 是 | — | — | — | — | 是 | 是 |
| 49 | `C_SE_NB_1` | `CONTROL_COMMAND` | `NONE` | 是 | — | — | — | — | 是 | 是 |
| 50 | `C_SE_NC_1` | `CONTROL_COMMAND` | `NONE` | 是 | — | — | — | — | 是 | 是 |
| 51 | `C_BO_NA_1` | `CONTROL_COMMAND` | `NONE` | 是 | — | — | — | — | 是 | 是 |
| 58 | `C_SC_TA_1` | `CONTROL_COMMAND` | `CP56TIME2A` | 是 | — | — | — | — | 是 | 是 |
| 59 | `C_DC_TA_1` | `CONTROL_COMMAND` | `CP56TIME2A` | 是 | — | — | — | — | 是 | 是 |
| 60 | `C_RC_TA_1` | `CONTROL_COMMAND` | `CP56TIME2A` | 是 | — | — | — | — | 是 | 是 |
| 61 | `C_SE_TA_1` | `CONTROL_COMMAND` | `CP56TIME2A` | 是 | — | — | — | — | 是 | 是 |
| 62 | `C_SE_TB_1` | `CONTROL_COMMAND` | `CP56TIME2A` | 是 | — | — | — | — | 是 | 是 |
| 63 | `C_SE_TC_1` | `CONTROL_COMMAND` | `CP56TIME2A` | 是 | — | — | — | — | 是 | 是 |
| 64 | `C_BO_TA_1` | `CONTROL_COMMAND` | `CP56TIME2A` | 是 | — | — | — | — | 是 | 是 |
| 70 | `M_EI_NA_1` | `INITIALIZATION` | `NONE` | — | — | — | — | — | — | — |
| 100 | `C_IC_NA_1` | `SYSTEM_COMMAND` | `NONE` | — | — | — | — | — | — | — |
| 101 | `C_CI_NA_1` | `SYSTEM_COMMAND` | `NONE` | — | — | — | — | — | — | — |
| 102 | `C_RD_NA_1` | `SYSTEM_COMMAND` | `NONE` | — | — | — | — | — | — | — |
| 103 | `C_CS_NA_1` | `SYSTEM_COMMAND` | `CP56TIME2A` | — | — | — | — | — | — | — |
| 104 | `C_TS_NA_1` | `SYSTEM_COMMAND` | `NONE` | — | — | — | — | — | — | — |
| 105 | `C_RP_NA_1` | `SYSTEM_COMMAND` | `NONE` | — | — | — | — | — | — | — |
| 106 | `C_CD_NA_1` | `SYSTEM_COMMAND` | `CP16TIME2A` | — | — | — | — | — | — | — |
| 107 | `C_TS_TA_1` | `SYSTEM_COMMAND` | `CP56TIME2A` | — | — | — | — | — | — | — |

其中 `QOI=20` 列严格表示当前未修改 `iec104-python` Server 的实际站总召筛选结果，不表示 IEC104 标准理论能力。

### 6.7 Whale 任务与视图

`task` 直接保存：

```text
连接
协议原生操作
IEC104_CONTROLLING_STATION / IEC104_CONTROLLED_STATION
任务点表
触发方式
超时与重试
任务状态
版本区间
```

`vw_task_full` 只输出当前任务摘要；`vw_task_point_item` 通过 `task_point_table_id` 输出成员点。协议 Point 参数由 `vw_iec104_point_item` 提供。

组召操作不进入可执行任务数据。多总召点集通过多个 Common Address / Station 实现。

## 7. 带时间数据

IEC104 可以发送带时间标签的数据，但不是任意 ASDU 都附加时间戳，而是选择带时标的 Type ID。

| 不带时间 | 带 CP56Time2a 时间 |
|----------|--------------------|
| `M_SP_NA_1` | `M_SP_TB_1` |
| `M_DP_NA_1` | `M_DP_TB_1` |
| `M_ME_NC_1` | `M_ME_TF_1` |
| `M_IT_NA_1` | `M_IT_TB_1` |

示例：

```yaml
- name: wind_speed
  ioa: 3001
  type_id: M_ME_TF_1
  data_type: float
  timestamp: CP56Time2a
```

`CP56Time2a` 常用于事件顺序记录、遥测时标和调度侧 SOE 分析。

## 8. `iec104-python` 公开对象的使用边界

本项目直接使用 `c104.Server`、`Station` 和 `Point`，不修改第三方包源码。典型初始化关系为：

```python
import c104

server = c104.Server(ip="0.0.0.0", port=2404)
station_1 = server.add_station(common_address=1)
station_2 = server.add_station(common_address=2)

point = station_1.add_point(
    io_address=1001,
    type=c104.Type.M_ME_NC_1,
    report_ms=5000,
)

server.start()
```

实际参数名应以项目锁定的 `iec104-python` 版本为准。数据库 Adapter 的职责是：

```text
vw_task_full
→ 创建或查找 Server / Connection
→ 根据 Common Address 创建 Station
→ 根据 vw_iec104_point_item 创建 Point
→ 设置 command mode、report_ms、related IO
→ 维护自发上送运行时状态
→ 按 BACKGROUND_SCAN 任务执行全量发送
```

主动发送使用 Point 的发送接口；需要批量发送时使用包公开的批量发送能力。Adapter 不应尝试给 Point 动态增加 `last_send_at` 等属性，而应维护独立状态对象。

对于总召，Adapter 只负责按 CA 把正确 Point 注册到正确 Station。总召确认、数据分组、发送和终止由当前 `iec104-python` Server 实现完成，不再自定义一套 interrogation handler。

## 9. 重连、初始化与异常处理

### 9.1 主站重连后

子站侧通常不主动发起 TCP 重连，而是监听端口等待主站重新连接。主站重新连接后，应重新 STARTDT，并通常重新总召。

子站侧应做到：

```text
TCP 会话断开后清理会话状态；
保留当前过程数据缓存；
等待主站重新连接；
收到 STARTDT 后允许数据传输；
收到总召后重新按当前值上送快照。
```

### 9.2 子站重启后

子站重启后可发送 `M_EI_NA_1` 初始化结束，提示主站重新同步状态。主站收到初始化结束后，通常应重新总召。

子站侧建议：

```text
启动后加载点表；
建立当前值缓存；
等待主站连接；
STARTDT 后发送初始化结束；
等待主站总召；
按总召配置返回当前快照。
```

### 9.3 数据质量

子站上送时应维护质量位，例如：

| 场景 | 质量建议 |
|------|----------|
| 本地数据有效 | good |
| 数据源中断 | invalid |
| 数据超时 | old data |
| 设备检修或闭锁 | blocked / substituted |

主站侧不能长期认为最后一次值永远有效，应根据链路状态、质量位、时间戳和刷新周期判断数据是否 stale。

## 10. 工程改进意见

基于前面讨论，IEC104 文档和实现应重点修改以下内容：

1. 控制站和被控站共用 `cfg_iec104_point_item`，使用不同点表实例；统一执行视图为 `vw_iec104_point_item`，任务角色由 `task.protocol_role_ref_id` 表达。
2. 不建立 `cfg_iec104_point_interrogation` 或 `cfg_iec104_point_publish_policy`；总召范围由 Station 注册点决定，CYCLIC 与 SPONTANEOUS 参数保存在 Point，BACKGROUND_SCAN 由任务表达。
3. `type_id` 只表示 ASDU 编码类型；COT 由运行时触发原因生成，不是点位固定属性。
4. QOI 只存在于 `C_IC_NA_1` 召唤命令的信息体中；标准响应数据通过 COT 表明站总召或组召来源，但当前包只执行 QOI=20 站总召。
5. 总召是一次性快照请求，不是订阅；一次总召可能返回多个 ASDU，但流程结束后不会持续发送同一批数据。
6. 参与总召的数据由子站/RTU/网关侧配置决定，不由主站点表单方面决定。
7. 当前 `iec104-python` Server 自动处理 QOI=20 站总召；Whale Adapter 负责 Point 注册、自发上送状态、背景扫描任务和控制命令业务处理。
8. 多主站场景按 TCP session 独立维护序号和召唤上下文；总召响应只返回给发起会话。
9. APDU 结构必须说明多个 Information Object 的 SQ=0、SQ=1 编码方式。
10. 带时间数据通过带时标 Type ID 表达，例如 `M_ME_TF_1`，而不是设置独立时标开关。

## 11. 安全注意

IEC104 本身通常不提供现代加密认证能力。工程部署时应避免将 `2404` 端口直接暴露在公网。

建议：

- 使用电力专网、VPN、专线或安全隔离装置；
- 限制主站 IP 白名单；
- 记录遥控遥调审计日志；
- 对命令方向增加权限、闭锁、二次确认与防误逻辑；
- 子站端对异常连接、频繁总召、异常遥控进行限流和告警；
- 对公网或跨域场景采用安全网关或加密隧道。

## 12. 参考资料

- Beckhoff：IEC 60870-5-10x ASDU Type ID 表
- Beckhoff：IEC104 telegram structure，APDU 起始字节、长度与控制域说明
- lib60870 用户指南：Interrogation command、QOI=20、组召唤与服务端处理逻辑
- Kepware / PTC IEC104 互操作说明：APDU 最大长度、t1/t2/t3、k/w 等工程参数
- VTScada IEC104 Addressing：ASDU Address、IOA 与地址格式说明
